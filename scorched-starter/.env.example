# Copy to .env and fill in. Never commit .env itself.

RESEND_API_KEY=
CONTACT_TO=
CONTACT_FROM=

# Supabase (Supabase project -> Settings -> API)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Admin dashboard password
ADMIN_PASSWORD=

# Signs admin/location session tokens (lib/admin-session.ts)
ADMIN_SESSION_SECRET=

# Signs customer login-link and session tokens (lib/customer-session.ts)
CUSTOMER_SESSION_SECRET=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Site URL (Stripe redirects, unsubscribe links, webhook callbacks)
NEXT_PUBLIC_SITE_URL=

SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_IDS=
SQUARE_WEBHOOK_SIGNATURE_KEY=
SQUARE_WEBHOOK_NOTIFICATION_URL=

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Meta Pixel (public identifier, safe to expose client-side)
NEXT_PUBLIC_META_PIXEL_ID=

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=
PLAID_TOKEN_ENC_KEY=

CRON_SECRET=


# --- Marketing: email + SMS ------------------------------------------------

# Sendblue. Keys come from the Sendblue dashboard or `sendblue show-keys`.
SENDBLUE_API_KEY_ID=
SENDBLUE_API_SECRET_KEY=
# Documented base is https://api.sendblue.com. https://api.sendblue.co is an
# alias that answers identically; either works.
SENDBLUE_BASE_URL=https://api.sendblue.com
# E.164. Must be a line that exists on the Sendblue account, and it is required
# on every single send.
SENDBLUE_FROM_NUMBER=
# Shared secret registered with the webhook, echoed back in sb-signing-secret.
SENDBLUE_WEBHOOK_SECRET=

# Sendblue Blue Ocean throughput limits. All configurable because the real
# numbers depend on what Sam negotiates, not on what the docs advertise.
SMS_NEW_CONTACTS_PER_DAY=50
SMS_NEW_CONTACTS_PER_HOUR=15
SMS_BURST_PER_SECOND=10
SMS_QUEUE_CAP=1500
# Carrier compliance limit that Sendblue says cannot be disabled: a line may
# not send more than this many consecutive outbound messages without a reply.
SMS_MAX_CONSECUTIVE_NO_REPLY=150

# Local hours in America/Denver. The window wraps midnight: 20 to 9 means no
# texts go out between 8pm and 9am.
SMS_QUIET_HOURS_START=20
SMS_QUIET_HOURS_END=9

# Marketing email sends from a separate subdomain so a spam complaint can never
# damage delivery of booking and waiver mail from scorchedstudio.com.
RESEND_MARKETING_FROM="Scorched Studio <hello@news.scorchedstudio.com>"
# Resend renamed audiences to segments. This still holds the id that the
# Resend dashboard now labels "segment".
RESEND_AUDIENCE_ID=
# Signing secret for the Resend webhook (Svix). Starts with whsec_.
RESEND_WEBHOOK_SECRET=

# The master gate. While this is anything but exactly "true", every provider
# call logs what it would have sent and returns a fake handle. Nothing reaches
# a real phone or inbox. Leave it false everywhere except production, and only
# after the manual checklist in docs/marketing/SETUP.md has been run.
MARKETING_LIVE=false
